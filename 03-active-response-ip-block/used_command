# === 1. SYMULACJA ATAKU (Kali Linux) ===
hydra -l root -P /usr/share/wordlists/rockyou.txt ssh://192.168.122.1 -t 4
gobuster dir -u http://192.168.122.X/ -w /usr/share/wordlists/dirb/common.txt

# === 2. KONFIGURACJA ZARZĄDZANIA (Wazuh Manager) ===
sudo nano /var/ossec/etc/rules/local_rules.xml
sudo nano /var/ossec/etc/ossec.conf
sudo systemctl restart wazuh-manager

# === 3. WERYFIKATION I DIAGNOSTYKA (Wazuh Agent) ===
sudo iptables -L INPUT -n -v
sudo tail -n 20 /var/ossec/logs/active-responses.log
